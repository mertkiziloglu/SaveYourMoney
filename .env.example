# SaveYourMoney — Environment Variables

# === Security ===
# API key for legacy authentication (required in production)
APP_API_KEY=changeme

# JWT secret key — minimum 32 characters recommended
JWT_SECRET=saveyourmoney-jwt-secret-key-for-development-only-change-in-production

# JWT token expiration in milliseconds (default: 24 hours)
JWT_EXPIRATION_MS=86400000

# === Service URLs (for metrics collection) ===
CPU_HUNGRY_SERVICE_URL=http://localhost:8081
MEMORY_LEAKER_SERVICE_URL=http://localhost:8082
DB_CONNECTION_SERVICE_URL=http://localhost:8083

# === Spring Profiles ===
# dev = H2 in-memory, relaxed security
# prod = PostgreSQL, strict security
SPRING_PROFILES_ACTIVE=dev

# === Database (production only) ===
# SPRING_DATASOURCE_URL=jdbc:postgresql://localhost:5432/saveyourmoney
# SPRING_DATASOURCE_USERNAME=analyzer
# SPRING_DATASOURCE_PASSWORD=changeme
